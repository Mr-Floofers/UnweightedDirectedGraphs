using System;
using Xunit;
using UnweightedDirectedGraphs;

namespace XUnitTestProject1
{
    public class UnitTest1
    {
        //[Theory]
        //[InlineData(new int[] { 5, 6, 2, 8, 9 }, new int[] { 5, 8, 6, 2 }, new int[] { 6, 9, 8, 9 })]
        //public void AddTest(int[] nodes, int[] from, int[] to)
        //{
        //    UnweightedDirectedGraph<int> graph = new UnweightedDirectedGraph<int>();
        //    for (int i = 0; i < nodes.Length; i++)
        //    {
        //        graph.Add(nodes[i]);
        //    }

        //    for (int i = 0; i < from.Length; i++)
        //    {
        //        graph.AddEdge(from[i], to[i]);
        //    }

        //    for (int i = 0; i < nodes.Length; i++)
        //    {
        //        Assert.True(graph.Search(nodes[i]) != null);
        //    }

        //    for (int i = 0; i < from.Length; i++)
        //    {
        //        foreach (Node<int> node in graph.Nodes)
        //        {
        //            Assert.True(node.Value.Equals(from[i]) && node.PointingTo.Contains(graph.Search(to[i])));
        //        }
        //    }


        //}


    }
}
